import 'package:flutter/material.dart';

/// Central color constants for the DigiTwin app.
/// All UI colors must reference these values — do not hardcode hex values elsewhere.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFFF9644);
  static const Color accent = Color(0xFFFFCE99);
  static const Color background = Color(0xFFFFFDF1);
  static const Color darkBrown = Color(0xFF562F00);

  static const Color white = Colors.white;
  static const Color black87 = Colors.black87;

  static const Color errorRed = Colors.red;
  static const Color successGreen = Colors.green;
  static const Color warningOrange = Colors.orange;
  static const Color infoBlue = Colors.blue;
}
